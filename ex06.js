// --- 6. Alterando Valores de Variáveis ---

// Crie uma variável chamada `contador` e atribua o valor "0". 
// Em seguida, incremente seu valor três vezes e exiba o resultado no console.

let contador = 0;
contador++;
contador++;
contador++;
console.log(contador);