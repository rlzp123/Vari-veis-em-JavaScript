// --- 8. Diferença entre "var", "let" e "const" ---

// Explique a diferença entre "var", "let" e "const" no contexto do escopo. 

// Em seguida, crie um exemplo de código para demonstrar essa diferença.



// Explicação:

// "var" é a forma meio que tradicional de declarar variáveis em JavaScript.

// Como, ela tem escopo de função, uma variável declarada com "var" dentro de uma função 
// só estará "disponível" dentro dessa função. 

// Além disso, "var" permite redeclaração e reatribuição de variáveis.